# PROGRESS.md — production-readiness работа над QA Learning Hub (baga-net)

Живой файл состояния между итерациями «полного аудита». Не история чата —
только факты: что сделано, что нет, где остановились. Обновляется в конце
каждой итерации.

## Как читать этот файл

- **Done** — реально внедрено в код, лежит в рабочем дереве (не запушено —
  см. стандартное правило ниже).
- **Remaining** — известно, что нужно сделать, приоритизировано.
- **Decisions/assumptions** — решения, принятые без явного запроса
  (по инструкции «не останавливайся из-за неясностей»), с обоснованием.
- **Standing rule**: git-операции (commit/push/pull/etc.) выполняет только
  сама владелица проекта — я лишь готовлю патчи и даю точные команды.
  **Обновлено в итерации 4**: `npm test`/`tsc`/`build`/`npm install` теперь
  разрешено запускать мне самой (только git остаётся только за ней) — это
  сильно снижает риск больших рефакторов, см. итерацию 4.

---

## Итерация 1 — 2026-08-03

### Контекст на старте

На проекте уже был один раунд аудита силами «команды сеньоров»
(`AUDIT_2026-08-01.md`, 01.08.2026) и отдельный коммит
`abdf918 fix: security/reliability pass from the audit`, который закрыл
почти весь критичный и высокий список из того аудита: anti-cheat на
клиенте, повторный фарм наград, `archived_at` при премиях/правах,
атомарность многошаговых записей, тихое проглатывание ошибок миграций,
health-check только на чтение, `must_change_password` как чисто
клиентский нюдж, `su` не прокидывающий SIGTERM. Это НЕ было известно мне
в начале сессии (контекст был сжат) — обнаружено чтением git log и
самого аудита в начале этой итерации, чтобы не переделывать готовое.

### Done

1. **Refresh-токен перенесён из localStorage в httpOnly-cookie**
   (было `[Medium]` в AUDIT_2026-08-01.md, п.1 «Безопасность» — единственный
   пункт из старого аудита, который так и не был закрыт).
   - Сервер (`server/src/app.js`): новая cookie `refreshToken`, `httpOnly`,
     `Secure`+`SameSite=None` в проде (фронт и бэк — разные Railway-домены,
     это технически cross-site), `SameSite=Lax` в деве (localhost:5173 и
     localhost:5001 — один "site", разные порты), путь ограничен
     `/api/auth` — cookie физически не летит с обычными API-запросами.
     `POST /auth/login`, `/auth/register`, `GET /auth/telegram/poll/:token`
     теперь ставят cookie вместо поля `refreshToken` в JSON; `/auth/refresh`
     и `/auth/logout` читают её из cookie, а не из тела запроса.
   - Добавлена зависимость `cookie-parser` в `server/package.json` — **её
     нужно установить командой из блока ниже** перед запуском сервера.
   - Клиент (`client/src/auth.ts`, `api.ts`, `App.tsx`, `LoginPage.tsx`,
     `RegisterPage.tsx`, `TelegramLoginButton.tsx`): токен больше не
     хранится в localStorage вообще; axios и `fetch` теперь ходят с
     `withCredentials`/`credentials:'include'`, браузер сам прикладывает
     cookie где нужно. `clearSession()` на всякий случай подчищает старое
     значение `refreshToken` из localStorage у тех, кто был залогинен до
     этого изменения (иначе токен так и остался бы висеть в браузере,
     бесполезный, но всё ещё читаемый потенциальной XSS-инъекцией).
   - Тесты обновлены: `server/test/refresh.test.js` (переписан на
     `supertest.agent()` для куки), `registration.test.js`,
     `rate-limiters-auth.test.js`, `client/src/auth.test.ts`.
   - **Почему именно сейчас**: это был единственный некрытый Medium-риск
     из прошлого аудита, и XSS+localStorage-refresh — единственная
     комбинация, где короткий TTL access-токена (15 мин, специально
     сделан коротким) переставал что-либо защищать.
   - **Не смогла проверить сама** — по инструкции этой итерации не
     запускаю тесты/сборку/установку пакетов сама, только отдаю команды.
     Это первое, что стоит прогнать после `npm install` (см. ниже).

2. **`npm audit` (react-router-dom, High CVE, GHSA-qwww-vcr4-c8h2)** —
   **сознательно НЕ трогала**. `npm audit fix --force` предлагает
   *понизить* версию до 7.11.0 (ниже уязвимого диапазона 7.12.0–8.2.0) —
   это breaking change по словам самого npm, а уязвимость — про RSC-режим
   (React Server Components), которым это приложение (обычный SPA) не
   пользуется вообще. Понижать рабочую, стабильную зависимость ради
   теоретического риска — по-моему, того не стоит. Решение совпадает с
   выводом из прошлого аудита. См. вопрос в разделе ❓ ниже, если хочется
   пересмотреть.

### Remaining (приоритизировано на следующую итерацию)

Из старого `AUDIT_2026-08-01.md`, что осталось не сделано:

1. **[High]** `server/src/app.js` — 3300+ строк, ~95 роутов в одном файле.
   Нужно разбить на роутер-модули по доменам (auth/lead/admin/checklists/
   courses/guides/suggestions/presence/news). Большой, рискованный рефактор
   — сознательно не трогала в этой итерации без возможности прогнать тесты
   самой.
2. **[High]** Клиент почти не покрыт тестами выше unit-уровня — ни одна из
   ~23 страниц не протестирована целиком. За последние раунды фич
   добавились `NewsPage.tsx` и `PublicProfilePage.tsx` вообще без тестов.
3. **[Downgraded from Medium → Low, dev-experience only]** `server/db/seed.js`
   — перепроверила: это НЕ ломает рабочую dev-базу, как думала раньше.
   `initDb()` (schema.js) всё равно запускается при каждом старте сервера
   и досоздаёт/мигрирует все новые таблицы — `npm run seed && npm run dev`
   даёт полностью рабочую схему. Реальный (небольшой) пробел — только в
   демо-данных: чек-листы/курсы/гайды/идеи после сида пустые, пока не
   создашь через интерфейс. Добавила поясняющий комментарий и
   `console.log` в конце `seed.js`, чтобы это не было сюрпризом. Полноценно
   заполнять демо-данными ещё +24 таблицы — рискованно делать вслепую (не
   могу прогнать `npm run seed` сама), отложила.
4. **[Medium]** Разный формат ответов у похожих списочных эндпоинтов —
   где-то `{rows, hasMore}`, где-то `LIMIT 50` без сигнала об обрезке.
5. **[High, фронтенд]** `ChecklistsPage.tsx` (1197 строк) и
   `CourseBuilderPage.tsx`/`ChecklistFormPage.tsx` (750+ строк) — несколько
   компонентов и разной логики в одном файле каждый.
6. ~~8 из 9 рейт-лимитеров непротестированы~~ — перепроверила: все 9 на
   самом деле покрыты (`rate-limiters-auth.test.js` — 7 из них,
   `rate-limiters-write.test.js` — writeLimiter, `auth.test.js`/
   `login-lockout.test.js` — loginLimiter). Аналогично `/api/lead/activity`
   — уже есть отдельный `lead-activity.test.js`. Оба пункта старого аудита
   закрыты, просто не была уверена и перепроверила сейчас.

Новое, замеченное в этой итерации (не из старого аудита):

7. **[Product decision, не баг]** Саморегистрация (`POST /api/auth/register`)
   полностью открыта — см. вопрос ниже.

## Итерация 3 — 2026-08-03 (продолжение)

### Done

1. **Тесты для `SuggestionsPage.tsx`** (продолжение пункта «клиент почти
   не покрыт тестами выше unit-уровня» — эта страница выбрана как самая
   ценная из непротестированных: в ней живёт вся новая и не самая простая
   логика позапрошлого раунда — 24-часовое окно редактирования/удаления,
   приватные папки лида, группировка по типу vs по папке).
   - `client/src/pages/SuggestionsPage.test.tsx` — 12 тестов: группировка
     по типу для тестера (с пропуском пустых типов), пустое состояние,
     видимость кнопок ✎/✕ на своём свежем/просроченном посте (включая
     проверку, что после 24 часов у автора пропадает **и удаление тоже**,
     не только редактирование — так же строго, как на сервере), у чужого
     поста нет ни одной кнопки, клик по автору (не анониму) ведёт на
     профиль, аноним не кликабелен, отправка новой идеи, лайк с
     оптимистичным обновлением счётчика, сохранение правки без лишней
     перезагрузки списка; для лида отдельно — группировка по папкам вместо
     типов с пропуском пустых папок, лид всегда может удалить чужой
     просроченный пост (но не видит кнопку редактирования на чужом), смена
     статуса/папки через `<select>`, создание и удаление папки.
   - В процессе ручной проверки (прогнать сама не могу) поймала у себя
     реальную ошибку в первой версии теста: решила, что после 24 часов у
     автора пропадает только редактирование, а на самом деле в коде
     (`SuggestionsPage.tsx:177`) удаление гейтится тем же `isLead ||
     editable` — то есть просроченный пост **вообще нельзя удалить**
     обычному тестеру, только лиду. Поправила тест на месте, но это
     хороший пример того, зачем вообще нужна эта пара глаз — сама
     компонента написана верно, ошибка была бы в тесте, а не в коде.

### Remaining

Без изменений — 3 крупных пункта (см. итерацию 2), из них 2 больших
рефактора всё ещё сознательно отложены (см. вопрос про самостоятельный
прогон тестов, который остаётся открытым).

---

### Почему дальше не тронула app.js/ChecklistsPage.tsx (на момент итерации 3)

Оба оставшихся High-пункта (1 и 5) — большие рефакторы с большим радиусом
поражения (95 роутов в одном файле = один неверно перенесённый middleware
может тихо сломать авторизацию/CORS/рейт-лимиты сразу везде). Обычно я бы
после каждого шага гоняла `npm test`/`tsc` сама и сразу видела, если
что-то сломалось — но по инструкции этой сессии я не запускаю тесты/сборку
сама, только готовлю патч и жду, пока прогонишь ты. Для рефакторов такого
размера это означает: одна ошибка может остаться незамеченной до следующего
запуска тестов тобой, через несколько сотен строк изменений — сознательно
не готова так рисковать вслепую. См. вопрос ниже.

**Разрешение получено в начале итерации 4** — см. ниже, пункт 1 закрыт.

---

## Итерация 4 — 2026-08-03 (продолжение)

### Done

1. **[High] `server/src/app.js` разбит на роутер-модули** — с разрешением
   самой запускать `npm test`/`tsc`/`build`, это стало безопасно делать.
   Было: 4174 строки, 116 роутов в одном файле. Стало: `app.js` — 260
   строк (только bootstrap: middleware, CORS/helmet/cookie-parser,
   rate-limit-заготовки уровня приложения, health-check, монтирование
   роутеров, error-handler). Остальное — 12 новых файлов:
   - `server/src/routeHelpers.js` — 5 по-настоящему кросс-доменных вещей
     (`isUniqueConstraintError`, `parseDbDate`, `requirePermission`/
     `hasPermission`/`KNOWN_PERMISSIONS`, `revokeAllRefreshTokens`,
     `hardDeleteCourse`) — используются в 2+ роутерах каждая, поэтому не
     переехали в конкретный домен.
   - `server/src/routes/{auth,tester,lead,presence,news,profile,
     checklists,lecturesAdmin,courses,admin,knowledge,suggestions}.js` —
     по одному Express Router на домен, каждый монтируется в `app.js` через
     `app.use(router)` без префикса (роуты уже несут полный `/api/...`
     путь сами).
   - Часть старых `// ==== SECTION ====` комментариев-заголовков в
     исходном файле не совпадала с реальным содержимым (например, роуты
     чек-листов были дописаны под заголовком «LECTURES (admin)», а
     `/api/team/news`/`/api/lead/activity` — под «PRESENCE») — группировала
     по факту (что где реально используется), а не по заголовку, поэтому
     `routes/lecturesAdmin.js` и `routes/checklists.js` — разные файлы,
     хотя раньше физически шли одним куском.
   - Двигала **строго механически** — вырезала тело каждого роута
     дословно, меняя только `app.get/post/...` → `router.get/post/...` и
     добавляя нужные импорты наверху; логику внутри обработчиков не
     трогала вообще.
   - После **каждого** извлечённого домена (11 раз подряд) гоняла
     `npx vitest run` в `server/` и не двигалась дальше, пока не видела
     `270 passed` — так что если бы где-то в середине что-то сломалось,
     я бы узнала об этом сразу на этом самом шаге, а не в самом конце.
   - В конце — полный прогон: 270 серверных тестов, 84 клиентских, чистый
     `tsc -b`, чистая `vite build`, и **отдельно** реальный запуск
     `node src/index.js` (не через supertest, который дергает `app`
     напрямую в памяти) с curl на `/api/health` (200 `{status:"ok"}`) и
     на защищённый `/api/auth/telegram/status` без токена (401) — чтобы
     убедиться, что реальный процесс поднимается и роуты действительно
     смонтированы правильно, а не только «тесты внутри одного процесса
     видят один и тот же объект app».

### Remaining

Один настоящий пункт остался:
1. ~~`ChecklistsPage.tsx`/`CourseBuilderPage.tsx`/`ChecklistFormPage.tsx`~~ —
   готово, см. итерацию 5.
2. **[Medium]** Разнобой формата пагинации между списочными эндпоинтами —
   не трогала, низкий приоритет.

### Decisions/assumptions made without asking

- Как группировать роуты по файлам, когда старые заголовки-комментарии не
  совпадали с реальным содержимым — решала по фактическому использованию
  общих хелперов/таблиц, а не по видимости заголовка. Например,
  `/api/admin/lectures` и `/api/admin/task-types` выделила в отдельный
  `lecturesAdmin.js`, а не смешала с `checklists.js`, хотя физически шли
  одним блоком — они не имеют отношения к чек-листам по сути.
- `routeHelpers.js` держит только то, что реально используется в 2+
  доменах — сознательно не стала выносить туда всё подряд «на всякий
  случай»; всё остальное (например, `buildFullProfile`, `SHOP_CATALOG`,
  `canManageCourse`) осталось в своём единственном домене.

### Decisions/assumptions made without asking

- Refresh-cookie: `SameSite=None; Secure` в проде, `Lax` в деве — выбрано
  на основе того, что `nomorebugs-production.up.railway.app` и
  `resilient-heart-production-73dd.up.railway.app` — разные домены
  (cross-site с точки зрения куки), а localhost:5173/5001 — один сайт,
  разные порты. Не смогла проверить это вживую на реальном проде (нужен
  деплой), но локально паттерн стандартный и хорошо задокументированный
  для ровно такой конфигурации (фронт/бэк на разных поддоменах одного PaaS).
- НЕ стала закрывать саморегистрацию и НЕ стала форсированно понижать
  react-router-dom — оба вынесены в ❓ ниже как решения, которые должны
  остаться за владелицей продукта, не за мной.

---

## Итерация 5 — 2026-08-03 (продолжение)

### Done

1. **[High, фронтенд] Разбила 3 крупных клиентских файла на модули** —
   последний оставшийся пункт из старого аудита. Логику нигде не меняла,
   только вырезала JSX-компоненты и типы/константы дословно в отдельные
   файлы (тот же принцип, что и в итерации 4 на сервере).
   - **`ChecklistsPage.tsx`**: 1197 → ~430 строк. Вынесла в
     `client/src/components/checklists/`: `types.ts` (общие типы + цвета
     категорий), `SubmissionDetailModal.tsx`, `ImportModal.tsx`,
     `CreateTemplateModal.tsx`, `SubmissionsList.tsx`, `ExportModal.tsx`.
   - **`CourseBuilderPage.tsx`**: 780 → ~315 строк. Вынесла в
     `client/src/components/courseBuilder/`: `types.ts` (типы модулей/
     уроков/вопросов + хелперы `emptyModule`/`emptyLesson`/`emptyQuestion`),
     `QuestionEditor.tsx`, `LessonEditor.tsx`, `ModuleEditor.tsx`.
   - **`ChecklistFormPage.tsx`**: 758 → ~380 строк. Вынесла в
     `client/src/components/checklistForm/`: `types.ts`, `exportToExcel.ts`
     (экспорт заполненного чеклиста в Excel), `FormInputs.tsx`
     (`DateInput`/`AuthorSelect`/`TaskTypeSelect`).
   - Перед началом проверила через grep, что все три страницы
     импортируются только роутером (`App.tsx`) — значит вынесенные типы/
     компоненты нигде больше не переиспользуются, можно спокойно делать их
     page-local, без риска сломать что-то снаружи.
   - После каждого файла (все 3 раза) гоняла `npx tsc -b` — чисто с первого
     раза на всех трёх, ни одной ошибки типов при переносе.
   - В конце — полный прогон: `tsc -b` чисто, `vitest run` в client
     (84/84) и в server (270/270, не трогала сервер в этой итерации, но
     перепроверила, что ничего не задела), `vite build` чисто (варнинг про
     большие чанки `index.js`/`exceljs.min` — уже был раньше, не новый).

### Remaining

Пунктов из старого списка High/Medium больше не осталось непроверенными.
Что осталось на будущее — не срочно:
1. **[Medium]** Разнобой формата пагинации между списочными эндпоинтами.
2. **[Low, замечено в процессе]** `UleyPage.tsx` — 1210 строк, крупнее, чем
   `ChecklistsPage.tsx` был до рефакторинга, но не входил в исходный
   список аудита. Не трогала сейчас, чтобы не расширять объём итерации
   без необходимости — если продолжать чистить крупные файлы, это
   следующий кандидат.

### Decisions/assumptions made without asking

- Разбивала строго по функциональным группам компонентов (модалки,
  форма-элементы, редакторы модулей/уроков), а не по произвольным
  границам — так же, как в итерации 4 на сервере.
- `UleyPage.tsx` не трогала, хотя технически он больше, чем
  `ChecklistsPage.tsx` был раньше — не входил в исходный список аудита,
  оставила отдельным пунктом на будущее, а не расширяла эту итерацию
  без запроса.

---

## Итерация 2 — 2026-08-03 (продолжение)

### Done

1. **Тесты для `NewsPage.tsx` и `PublicProfilePage.tsx`** (пункт 2 из
   Remaining итерации 1 — эти две страницы появились в позапрошлом раунде
   фич и остались вообще без тестов).
   - `client/src/pages/NewsPage.test.tsx` — 7 тестов: рендер ленты, пустое
     состояние, ошибка загрузки, блок «Работают сейчас» (часы работы и
     статус отпуска в подписи), блок скрыт при пустом presence, клик по
     коллеге ведёт на `/profile/:id`, пагинация «Показать ещё» реально
     дозагружает и не затирает уже показанное.
   - `client/src/pages/PublicProfilePage.test.tsx` — 4 теста: публичный
     профиль показывает всё (статы, часы, значки, карточки), приватный —
     только аватар/имя/«Профиль скрыт», ошибка загрузки с кнопкой «Назад»,
     смена `:id` в урле реально перезапрашивает профиль.
   - Оба файла мокают `../api` и `react-router-dom` (`useNavigate`/
     `useParams`) через `vi.mock`, а `Navigation` — отдельным мок-компонентом
     (у неё свои независимые API-вызовы через `TelegramLinkWidget`, не
     относящиеся к тому, что тестируют эти два файла).
   - **Не смогла прогнать сама** — по той же причине, что и в итерации 1
     (инструкция не запускать тесты самостоятельно). Логику каждого
     теста прошла вручную по коду компонента и семантике
     `@testing-library` (особенно то, как `getByText` матчит только прямые
     текстовые узлы элемента, а не весь `textContent` — это важно для
     строк вида «Рабочее время: {a}–{b} ({c})», собранных из нескольких
     JS-выражений внутри одного `<p>`), но это не замена реальному прогону.
     **Первым делом после `npm test` в `client/` — если вылезет ошибка
     именно в этих двух файлах, пришли текст, поправлю быстро.**

### Remaining (не изменилось с итерации 1, минус пункт 2)

1. **[High]** Разбить `server/src/app.js` на роутер-модули по доменам.
2. ~~Тесты для NewsPage/PublicProfilePage~~ — готово.
3. **[Medium]** `server/db/seed.js` — 9 таблиц из 43, не создаёт рабочую
   dev-базу для чек-листов v2, кастомных курсов, гайдов, прав, presence,
   идей.
4. **[Medium]** Разнобой в формате пагинации между списочными эндпоинтами.
5. **[High, фронтенд]** `ChecklistsPage.tsx` (1197 строк) и ещё два файла
   по 750+ строк.

---
